#include<stdio.h>
int main()
{
    int princical=1000,rate=2,years=1;
    int simpleinterest=(princical*rate*years)/100;
    printf("the value of simpleinterest %d",simpleinterest);
    return 0;
}