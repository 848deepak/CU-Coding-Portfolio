#include<stdio.h>
int main()
{
    int length, breadth;
    printf("enter the value of length is \n");
    scanf("%d", &length);
    
    printf("enter the value of breadth is \n");
    scanf("%d", & breadth);

    printf("the area of rectangle is %d",length*breadth);
    return 0;
}